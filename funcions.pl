member(X, L).
nth1(K, L, F). % K = 1, F = L[1]
last(L, X).
append(L1, L2, R).
sum_list(L, S).
sort(LNO, LO).
reverse(L, R).
write(...).
length(L, X).
select(X, L, R). % R = L - {X}
permutation(L, P).
subtract(L, S1, S2). % S2 = L - S1

% !
% fail
% nl
